#ifndef ALGORITHM_H
#define ALGORITHM_H

#include "struct.h"
#include <math.h>
#include <stdlib.h>
#include <stdio.h>

int algorithm(Point *points, Edge *edges, int point_count, int edge_count, int algorithm_id);

#endif
